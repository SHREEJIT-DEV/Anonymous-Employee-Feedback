{
  "name": "anonymous-employee-feedback-root",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "api",
    "bboard-cli",
    "bboard-ui",
    "contract"
  ],
  "scripts": {
    "dev": "npm --prefix ./bboard-ui run dev --",
    "build": "npm --prefix ./bboard-ui run build --",
    "typecheck": "npm --prefix ./bboard-ui run typecheck --",
    "preview": "npm --prefix ./bboard-ui run preview --"
  }
}